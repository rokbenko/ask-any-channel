INSTANCE_MODE=selfhost

# Postgres (docker-compose reads POSTGRES_PASSWORD; keep DATABASE_URL in sync if you change it).
# Use 127.0.0.1 rather than localhost: compose publishes the port on IPv4 loopback only, and
# `localhost` may resolve to ::1 first, costing a failed connection attempt on every command.
POSTGRES_PASSWORD=aac
DATABASE_URL=postgresql://aac:aac@127.0.0.1:5432/askanychannel

# Required for `aac dataset build` / `aac ingest` / `aac search` (embeddings).
# Not needed for `aac dataset load` when the bundle already contains matching embeddings.
OPENAI_API_KEY=
# Optional: any OpenAI-compatible endpoint. Leave blank for api.openai.com.
OPENAI_BASE_URL=

# Reserved for Phase 2 (chat). Leave blank for now.
ANTHROPIC_API_KEY=
